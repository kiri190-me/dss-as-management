# 통합 알림 · 사용자 관리 설계

「같은 사람이 **어느 시스템에 있든 같은 알림**을 받게 한다」는 요구에서 시작한 설계다.
조사하는 동안 **사용자 관리를 어디에 둘 것인가**가 같은 문제의 다른 얼굴이라는 것이 드러나
둘을 한 문서에 담는다.

작성 2026-09-21. 근거는 같은 날 돌린 **읽기 전용 조사 둘**이다(알림 현황 · 사용자와 권한 현황).
이 문서는 **설계 승인용**이고, 구현은 조각마다 따로 승인받는다.

🔴 **아직 사용자 결정이 안 난 항목이 있다.** B절에 모아 두었다. 그것이 정해지기 전에는
H절의 조각 1 이후로 넘어가지 않는다.

---

## A. 왜 하는가

사용자 요구 그대로다 — **「동일한 사용자가 모든 시스템에서(통합로그인에서도) 어딜 가든
같은 알람들을 받을 수 있도록 해야 할 것 같다.」**

지금은 **A/S 에만 알림이 있다.** 포털·계측기·개선요청에는 종이 없다. 그래서 A/S 밖에 있는
동안에는 결재가 와도 모른다.

---

## B. 정해진 것 · 정해야 할 것

### B-1. ✅ 정해진 것 (2026-09-21 사용자 결정)

| # | 물음 | 결정 | 따라오는 것 |
|---|---|---|---|
| 1 | **「어딜 가든 같은 알림」이 어느 쪽인가** | **종을 열면 모든 시스템의 알림이 한 자리에 보인다. 알림을 누르면 그것을 해소할 화면으로 이동한다.** 가만히 있어도 알림창이 뜨는 것은 **필요 없다** | 🔴 **D-4(브라우저 알림 권한)가 범위에서 빠진다** — 일이 크게 줄었다. 대신 **F-9(로그인을 거쳐도 원래 주소로 도착)가 필수**가 되었다 |
| 4 | **권한 회수 구멍**(G-4 ①)을 언제 고치나 | **사용자 관리 화면을 고친 다음에.** 지금은 아니다 | H절 조각 순서에 반영했다 |
| 2 | **알림 설정을 어디서 하는가** | 🔴 **포털에 창구 화면을 둔다. 자료는 각 시스템이 그대로 갖는다.** 통합 알림을 만드는 **그 작업 안에** 함께 넣는다 | **사용자가 사용자 관리를 빼려 한 주된 이유가 바로 이것이었다**(2026-09-21). D-5 |
| 2-b | **개인이 알림을 끌 수 있어야 하는가** | **아니다 — 지금처럼 역할 단위로 충분하다** | 🔴 **사람별 설정 표를 만들지 않는다.** 이 조각은 **스키마를 한 글자도 건드리지 않는다** |

| 3 | **A/S 「사용자 관리」 화면의 새 이름** | **「권한 · 승인 설정」** | 알림 탭이 떠나면 남는 넷(출하 대표 · 결재선 · 역할별 권한 · 개발자 표시)이 전부 「누가 무엇을 할 수 있나」다 |

### B-2. 아직 정해야 할 것

**없다.** 2026-09-21 로 설계가 확정되었다. 이제 조각 1부터 구현으로 넘어간다.

조각을 시작할 때마다 **그 조각의 지시서를 따로 승인받는다** — 이 문서가 구현 승인은 아니다.

---

## C. 지금 상태 (실측)

### C-1. 🔴 A/S 에는 알림을 담는 표가 없다

이 설계 전체를 좌우하는 사실이다.

> 「알림 테이블이 없다. 알림 행을 따로 쌓지 않고, 이미 있는 업무 데이터에서 **매 요청마다
> 다시 계산**한다. 그래도 되는 이유는 대부분이 **행동을 요구하는 알림**이기 때문이다.
> 결재를 처리하면 그 건은 다음 조회에서 저절로 빠진다」
> — `src/lib/domain/notifications.ts:3-10`

**알림 8종** (`domain/notifications.ts:39-48`) 중 **6종이 「내가 처리할 일」**이고 2종이 결과 통보다.

| 성격 | 종류 | 사라지는 방식 |
|---|---|---|
| 할 일 (6) | 접수건 결재 · 부품 요청 대기 · 재고 부족 · 새 수리 의뢰 · 불출 승인 대기 · 견적서 결재 대기 | **처리하면 저절로** |
| 결과 (2) | 승인 완료 · 반려됨 | 눌러서 확인하면(기록 표에 남는다) · 7일 지나면 |

🔴 **결재 mutation 은 알림을 한 줄도 쓰지 않는다.**
`queries/approval-outcome-notifications.ts:32-33` — 「이 파일은 아무것도 쓰지 않는다.」

이름이 `notification_*` 인 표 셋이 있지만 **전부 알림 본체가 아니다**:

| 표 | 무엇 |
|---|---|
| `notification_acknowledgements` | 정보성 알림을 **확인했다는 기록**(파생할 때 걸러 낸다) |
| `notification_kind_settings` | 알림 종류를 켜고 끔 |
| `notification_role_settings` | 어느 역할이 그 종류를 받는지 |

### C-2. 다른 시스템의 알림

| 시스템 | 알림 표 | 종 UI |
|---|---|---|
| 포털 `dss-auth` | **없다**(소스에 `notification` 글자가 0건) | 없다 |
| 계측기 `njlee` | **다른 것이 있다** — 교정 기한을 **월 1회 메일**로 보내는 배치(`web_notifications` 는 발송 기록) | 없다 |
| 개선요청 `dss-improvements` | 없다 | 없다 |

계측기의 것은 **사용자별 종·읽음 개념이 아예 없다.** 같은 물건으로 볼 수 없다.

### C-3. 지금 시스템 사이에 오가는 것

**딱 둘뿐이다.**
1. **사이트 → 포털**: OIDC 토큰 교환(로그인)
2. **포털 → 사이트**: **백채널 로그아웃 푸시** — 서명된 토큰을 `POST` 로 민다.
   세 사이트 모두 받는 엔드포인트가 있다. 타임아웃 5초, 실패해도 로그아웃은 성공시킨다.

🔴 **2번이 중요하다.** 「포털이 각 사이트를 부르는 길」이 **이미 검증된 채로 있다.**
인증 방식(서명 토큰)도, 대상 고르는 판정도, 실패를 삼키는 관례도 이미 정해져 있다.

### C-4. `@dss/ui`(메뉴바 묶음)의 제약

🔴 **이 묶음은 네트워크를 쓰면 시험이 막는다.** `dss-ui/src/service-menu/no-network.test.ts` 가
소스를 읽어 `fetch(` · `XMLHttpRequest` · `WebSocket` · `EventSource` · 박힌 주소 · `process.env` 를
전부 찾아낸다. README 1절: 「🔴 **자료를 가져오지 않는다.** fetch 도, 포털 주소도, 로그인도,
DB 도 모른다. **받은 목록을 그리기만 한다.**」

→ 종을 이 묶음에 넣는 것은 가능하지만 **자료는 반드시 prop 으로 받아야 한다.**

### C-5. 포털이 이미 「신원만」 담당하도록 설계돼 있다

> 「**역할(role)과 권한은 여기에 두지 않는다.** 각 시스템이 자기 DB에서 관리하고 `users.id` 로만
> 이어 붙인다. dss-auth 가 답하는 질문은 딱 하나다 — **'이 사람이 우리 회사 사람이고,
> 지금 쓸 수 있는 상태인가.'**」 — `dss-auth/src/lib/db/schema/users.ts:17-19`

**다만 역할은 이미 포털에 있다.** `user_client_grants.role` 이 **시스템마다 따로** 붙고,
A/S 는 매 로그인마다 그 값으로 자기 `users.role` 을 덮어쓴다.

> 「⚠️ 여기서 정한 역할은 받는 시스템의 권한을 그대로 결정한다. … 이 화면에서 SUPER_ADMIN 을
> 고르면 그 사람은 다음 로그인에 A/S 최고관리자가 된다.」 — `dss-auth/.../admin-access.ts:50-55`

---

## D. 알림 — 어디에 둘 것인가

### D-1. 후보 셋과 판정

| | 방식 | 판정 |
|---|---|---|
| ⓐ | **포털이 알림을 저장한다.** 각 시스템이 API 로 보낸다 | ❌ |
| ⓑ | 각 시스템이 자기 알림을 갖고, **종이 여러 곳을 돌며** 모은다 | ❌ |
| ⓒ | 알림 표 하나를 모두가 **직접 본다**(DB 공유) | ❌ |
| **ⓓ** | **각 시스템이 지금처럼 계산하고, 포털이 「모아 주는 창구」가 된다. 저장하지 않는다** | ✅ **권장** |

### D-2. ⓐ 를 버린 까닭 — 공짜가 전부 일이 된다

지금은 결재를 처리하면 **아무것도 안 해도** 알림이 사라진다(C-1). 저장 방식으로 바꾸면:

- 모든 결재·요청 mutation 에 **「알림 보내기」**를 넣어야 한다 — 지금은 한 줄도 없다
- 처리됐을 때 **「알림 지우기」**를 또 보내야 한다 — 사건이 두 배
- 한 군데라도 빠뜨리면 **이미 처리한 일이 종에 영영 남는다**

「처리하면 사라진다」는 성질은 **자료가 곧 진실**이기 때문에 성립한다. 그 성질을 버리는 값이
통합의 이득보다 크다.

### D-3. ⓑ · ⓒ 를 버린 까닭

**ⓑ**: 종을 열 때마다 시스템 수만큼 호출이 나간다. 한 곳이 느리면 종 전체가 느려지고,
「안 읽은 개수」를 세려면 매번 전부 물어야 한다. 시스템이 늘수록 나빠진다.

**ⓒ**: PO/내자 설계서 D절이 「**DB 를 함께 보는 것은 이번만이다.** 앞으로는 API 로 잇는다」고
못 박았다. 알림은 그 예외 조건(한 화면에서 섞어 정렬 · 한 트랜잭션 · FK)에 해당하지 않는다.

### D-4. ✅ 범위에서 빠진 것 — 「자동으로 뜨는 것」

🔴 **B-1 의 결정으로 이 절은 이번 설계의 범위가 아니다.** 종을 열면 보이는 것으로 충분하다.
아래는 **나중에 「자동으로 떠야 한다」가 필요해질 때를 위해** 남겨 두는 기록이다.

지금 A/S 의 갱신 방식은 **1분 주기 새로고침**인데,
🔴 **그 타이머는 브라우저 알림 권한을 허락한 사람에게만 돈다**(`BrowserNotifications.tsx:345-359`).
허락하지 않은 사람이 새 알림을 아는 계기는 **종을 여는 것**과 페이지 이동뿐이다.

그리고 **웹 푸시가 아니다** — 「이 시스템은 사내망 http 이고 나중에 인터넷 없는 NAS 에서 도는
것이 전제라 그 갈래는 보류돼 있다」(`BrowserNotifications.tsx:47-52`).

→ **B-1 의 답이 ⓑ(자동으로 떠야 한다)라면 이 구조부터 손대야 한다.** 별도의 큰 조각이다.

---

### D-5. 🔴 알림 **설정**은 창구를 포털에 둔다 — 같은 원리, 같은 답

**사용자가 「사용자 관리를 밖으로 빼야겠다」고 생각한 주된 이유가 바로 이것이었다**(2026-09-21).
그래서 이 항목은 곁다리가 아니라 **요구의 한복판**이다.

**자료를 옮기는 것은 안 된다** — 알림 종류 8가지도, 역할 5가지도 **A/S 고유의 것**이다.
포털이 그것을 가지면 시스템이 늘 때마다 포털을 고쳐 배포해야 한다(D-3 의 ⓒ 와 같은 값).

**화면만 옮긴다.** 포털이 각 시스템에 「네 알림 설정 목록 내놔」라고 묻고, 받은 것을 그려 주고,
저장하면 그대로 돌려보낸다. **포털은 그 안에 무엇이 들었는지 알 필요가 없다.**

```
포털 › 관리 › 알림 설정
  A/S 관리 시스템
    접수건 결재 대기   [켬]   받는 역할: 관리자 · 최고관리자
    재고 부족         [켬]   받는 역할: 재고담당 · 관리자
  개선요청 · 계측기
    (알림 없음)
  ※ 저장하면 각 시스템에 전달된다. 값의 주인은 각 시스템이다.
```

🔴 **앞서 「나중에」라고 한 판단을 바꾼다.** 통합 알림이 생기면 **다른 시스템에도 알림이 생길 것**이고
(개선요청의 「내 요청에 답이 달렸다」 같은), 그때 설정이 흩어진다.
**통합 알림을 만드는 그 작업 안에 창구를 함께 넣는다.** 나중에 따로 만드는 것보다 싸다.

✅ **B-2-b 결정(역할 단위로 충분)** 덕분에 이 조각은 **스키마를 건드리지 않는다.**
지금 있는 두 표(`notification_kind_settings` · `notification_role_settings`)를
**읽고 쓰는 통로만** 열면 된다. 새 표도, 마이그레이션도 없다.

---

## E. 구조 (권장안 ⓓ)

```
각 시스템          「이 사람의 지금 알림」을 내주는 읽기 전용 API 하나
  A/S              (지금 listMyNotifications 를 그대로 내보낸다)
  계측기·개선요청   (나중에 알림이 생기면 같은 모양으로 붙인다)
      ↑ 서명 토큰으로 묻는다 (백채널 로그아웃과 같은 방식)
      │
   포털            등록된 시스템에 물어 합친다. 저장하지 않는다. 짧게만 캐시
      ↑
      │ 각 사이트의 layout 이 서버에서 한 번 부른다
   종 (@dss/ui)    받은 목록을 그리기만 한다 (C-4 제약)
```

**좋은 점**
- 종은 **한 곳만** 부른다. 새 시스템이 생겨도 **종 코드는 안 바뀐다**
- 각 시스템의 계산 로직·확인 기록·설정을 **그대로 둔다**
- 「처리하면 사라진다」가 공짜로 유지된다
- A/S 의 종은 이미 **화면 틀이 서버에서 가져와 넘겨주는** 모양이다(`app/(app)/layout.tsx:79`).
  가져오는 곳만 바꾸면 된다

**치르는 값**
- 포털이 죽으면 알림이 안 온다(다만 포털이 죽으면 로그인도 안 된다)
- 포털에 **캐시가 필요하다** — `listMyNotifications` 는 페이지 렌더마다 최대 8개 조회를 돌리고,
  거기에 1분 폴링과 시스템 수가 곱해진다
- 포털에는 알림 관련 코드가 **하나도 없다.** 전부 새로 만든다

---

## F. 어려운 문제와 답

| # | 문제 | 답 |
|---|---|---|
| 1 | **알림 링크가 전부 상대경로다**(`/repair-cases/...`). 다른 사이트에서 누르면 엉뚱한 곳으로 간다 | 각 시스템이 **API 로 내줄 때 자기 주소를 붙인다.** 포털 `clients.launcher_url` 에 `{lan}` 이 펼쳐진 완성 주소가 이미 있다 |
| 2 | **사람 식별자가 시스템마다 다르다.** A/S `users.id` · 계측기·개선요청 `web_users.id` · 포털 `users.id` | 묻는 열쇠는 **포털 `users.id`(토큰의 `sub`)** 하나로 고정한다. 각 시스템이 자기 `sso_subject`/`auth_sub` 로 되짚는다 |
| 3 | 🔴 **A/S 만 포털 계정 없이도 되는 로컬 계정을 허용한다**(`sso_subject` 가 비어 있을 수 있다) | 그 사람은 **통합 종에서 빠진다.** A/S 안에서는 그대로 받는다. 이대로 둘지 확인 필요 |
| 4 | **역할 어휘가 시스템마다 다르고 겹치는 것이 없다**(A/S 5종 · 계측기 2종 · 개선요청 2종. `ADMIN` 만 글자가 같고 뜻이 다르다) | **포털은 역할을 판정하지 않는다.** 각 시스템이 자기 역할로 걸러 낸 결과만 내준다 |
| 5 | **확인 기록의 키 형식이 좁다**(`/^([A-Z][A-Z0-9_]*):[A-Za-z0-9:_-]+$/`, ASCII 200자) | 확인 기록은 **각 시스템에 그대로 둔다.** 파생 방식을 유지하므로 건드릴 일이 없다 |
| 6 | **알림 설정이 A/S 안에 있다**(사용자 관리 탭 4) | 🔴 **B-2 미결.** 창구만 포털로 모으고 주인은 각 시스템에 두는 길이 있다(G-3 과 같은 모양) |
| 7 | 포털이 각 시스템에 물을 때 **무슨 자격으로** | 백채널 로그아웃의 **서명 토큰 방식을 뒤집어** 쓴다. 새로 설계하지 않는다 |
| 8 | 한 시스템이 **느리거나 죽었을 때** | 백채널 로그아웃의 관례를 따른다 — **타임아웃을 짧게 두고, 실패는 삼키고 나머지를 보여준다.** 종이 통째로 안 뜨는 것이 가장 나쁘다 |
| **9** | 🔴 **로그인을 거쳐도 원래 가려던 화면에 도착해야 한다.** B-1 의 결정(「누르면 해소 화면으로 이동」)이 만든 필수 항목이다. A/S 에서 PO 의 알림을 눌렀는데 그날 PO 에 처음 가는 것이면 통합로그인이 한 번 돌고, 그때 **원래 주소를 잊어버리기 쉽다** — 「알림을 눌렀는데 엉뚱한 첫 화면이 떴다」로 나타난다 | 각 사이트의 로그인 시작 통로가 **가려던 주소를 들고 갔다가 끝나면 그리로 보낸다.** 🔴 **지금 만들고 있는 PO 사이트에도 필요하다** — 조각 2 전에 넣는다. 🔴 되돌아갈 주소는 **자기 사이트 안**이어야 한다(밖으로 보내면 피싱 통로가 된다) |

---

## G. 사용자 관리 — 무엇을 어디로

### G-1. 🔴 결론: 밖으로 뺄 것이 거의 없다

**진짜 사용자 관리는 이미 포털에 있다**(C-5). A/S 「사용자 관리」 화면에서 **할 수 없는 일**:

- 사용자 만들기 ✗ (SSO 로 자동 생성만)
- 이름·이메일·**역할** 고치기 ✗ (포털이 매 로그인마다 덮어쓴다)
- 승인·정지 ✗
- 부서·사번 ✗ (A/S 에 칸 자체가 없다)

### G-2. 진짜 문제 — 그 화면의 2/3이 사용자와 무관하다

| 탭 | 실제 내용 | 사용자 관리인가 |
|---|---|---|
| 1 출하 대표자 / 위임 | 대표 지정 · 대리승인 · 계정 삭제 | 절반만 |
| 2 **승인 절차** | 결재선(출하 · 불출 · 견적서) | ✗ A/S 업무 설정 |
| 3 역할별 접근 권한 | 5역할 × 21영역 | A/S 권한 정책 |
| 4 **알림 설정** | 종류 켜고 끄기 · 역할별 수신 | ✗ A/S 정책 |
| 5 개발자 표시 | | 사용자 |

줄 수로 **사용자와 무관한 것 약 1,782줄 / 사용자 관련 983줄**.
화면 제목도 이미 어긋나 있다 — 「사용자 관리」인데 부제는 출하 대표 얘기만 한다.

각 탭이 **왜 거기 있는지는 주석에 다 적혀 있다**(요구가 「사용자 관리에서」였고, 계정을 보다가
「이 역할은 어디까지 되지?」를 확인하는 흐름이라서). **그 판단 자체는 틀리지 않았다.**
틀린 것은 **이름**이다.

### G-3. 권고 — **알림 탭 하나만 떠나고**, 나머지는 이름을 바꾼다

🔴 **2026-09-21 에 요구의 뿌리가 밝혀졌다** — 사용자 관리를 빼려 한 **주된 이유가 알림**이었다.
그래서 답이 「전부 그대로 두고 이름만」에서 **「알림 탭 하나는 실제로 떠난다」**로 바뀌었다.

| 탭 | 어디로 | 까닭 |
|---|---|---|
| **알림 설정** | 🔴 **포털로**(화면만. 자료는 A/S 가 계속 갖는다 — D-5) | 관리자가 여러 시스템을 오가야 하는 **유일한 것** |
| 출하 대표자 · 위임 | A/S 에 남는다 | 「누가 출하를 승인하나」는 A/S 업무다 |
| 승인 절차(결재선) | A/S 에 남는다 | 출하 · 불출 · 견적서 결재선. 전부 A/S 업무 |
| 역할별 접근 권한 | A/S 에 남는다 | A/S 화면 21군데에 대한 권한. 포털이 알 수 없다 |
| 개발자 표시 · 계정 삭제 | A/S 에 남는다 | A/S 안에서 권한을 올리고, A/S 일감을 후임에게 넘기는 일 |

그리고:

1. **남는 넷에 맞게 이름을 바꾼다.** 알림이 빠지면 전부 「누가 무엇을 할 수 있나」라서
   **「권한 · 승인 설정」**이 정확해진다(B-2 에서 고른다).
2. **첫 탭 맨 위에 포털 안내 한 줄**을 둔다 — 「사람을 추가하거나 역할을 바꾸거나 계정을
   정지하려면 **포털에서** 합니다 [포털 열기]」. 지금은 관리자가 A/S 에서 찾다가 헤맨다.
   PO/내자 설계서 F-1 이 결재선 설정에 쓴 것과 같은 방법이다.
3. 🔴 **「계정 삭제」 옆에 설명을 적는다.** A/S 의 삭제는 **차단이 아니라** 목록 정리이고,
   포털로 다시 들어오면 되살아난다(2026-09-13 결정). 화면에 그 말이 없어 오해를 부른다.
4. **주인은 그대로, 창구만 하나로.** 권한을 포털로 **옮기지는 않는다** — 옮기면 포털이
   모든 시스템의 업무를 알아야 하고, 새 시스템마다 포털을 고쳐 배포해야 한다.

### G-4. 🔴 조사가 찾아낸, 지금 존재하는 구멍 셋

| # | 구멍 | 무게 |
|---|---|---|
| 1 | **권한을 회수해도 이미 로그인한 세션이 안 끊긴다.** 계정 **정지**는 각 시스템에 통보가 가는데(백채널 로그아웃), **권한 회수에는 그 호출이 없다.** A/S 8시간 · 계측기는 설정값만큼 그대로 산다 | 고치는 값이 **한 줄**이라 안 고칠 이유가 없다 |
| 2 | **`--open-to-all` 이 재실행마다 기본값으로 돌아간다.** 다른 인자는 「안 주면 유지」인데 이것만 예외다. 역할 하나 더하려고 등록 명령을 다시 돌리면 **전 직원 공개 시스템이 조용히 닫혀 전원 차단** | 이미 한 번 겪은 사고 |
| 3 | **삭제 동작이 시스템마다 정반대.** A/S 는 지운 계정이 다시 로그인하면 **되살아나고**(2026-09-13 결정), 계측기·개선요청은 **거절한다** | 둘 다 이유가 있어 지금 틀린 것은 아니다. **알고 있어야 하는 것** |

### G-5. 덤 — 부서·사번은 죽은 칸이다

포털이 부서(`department`)·사번(`employee_no`)을 **받아서 저장하지만 밖으로 한 번도 안 나간다.**
ID 토큰에도 `/userinfo` 에도 없다. 나머지 세 시스템에는 그 칸 자체가 없다.

→ 「부서별로 알림을 보낸다」 같은 것을 하려면 **클레임을 새로 추가**해야 한다.
추가 자체는 안전한 길로 이미 검증돼 있다(모르는 클레임은 무시되므로 옛 사이트가 안 깨진다).
🔴 다만 **사설 클레임에는 접두사를 붙이는 규약**이 있다(`dss_services` 처럼).

---

## H. 조각 순서

각 조각은 **혼자 커밋할 수 있고, 거기서 멈춰도 시스템이 돈다.**

🔴 **알림 설정 창구가 요구의 한복판이라 순서가 바뀌었다**(D-5). 포털에 설정 화면이 생긴
**뒤에야** A/S 에서 그 탭을 걷어낼 수 있다 — 먼저 걷어내면 설정할 곳이 없어진다.

| 조각 | 내용 | 위험도 | 선행 |
|---|---|---|---|
| **0** | ✅ **설계 확정** — 2026-09-21 완료. 새 이름은 「권한 · 승인 설정」 | — | — |
| **1** | 🔴 **로그인을 거쳐도 원래 주소로 도착하게**(F-9). **PO 사이트부터** | MEDIUM | 없음 — **지금이 가장 싸다** |
| **2** | A/S 가 **알림을 내주는 통로**와 **설정을 읽고 쓰는 통로**를 연다. 링크에 자기 주소를 붙인다(F-1). **화면은 안 바꾼다** | MEDIUM | 없음 |
| **3** | 포털이 **알림을 모아 주는 창구**를 만든다. 캐시 · 타임아웃 · 실패 삼키기 | MEDIUM | 2 |
| **4** | 🔴 **포털에 알림 설정 화면**(D-5). 스키마 변경 없음 | MEDIUM | 2 |
| **5** | 종을 `@dss/ui` 로 옮긴다. **자료는 prop 으로만**(C-4) | MEDIUM | 3 |
| **6** | 각 사이트가 그 종을 붙인다 | LOW | 5 · 1 |
| **7** | **A/S 에서 알림 설정 탭을 걷어내고**, 화면 이름을 바꾸고, 포털 안내 한 줄을 넣는다(G-3). 「삭제는 차단이 아니다」 설명도 함께 | LOW | 4 · 0 |
| **8** | **권한 회수가 세션을 끊게 한다**(G-4 ①). 이미 있는 통보 장치를 한 번 더 부른다 | MEDIUM | 7 — **사용자 결정** |
| — | ~~자동으로 뜨게 하기~~ | **범위 밖** (B-1) | — |
| — | ~~사람별 알림 설정~~ | **범위 밖** (B-2-b) | — |

🔴 **조각 1은 다른 것과 독립이고 지금이 가장 싸다.** PO 사이트가 아직 뼈대뿐이라
로그인 통로를 지금 고치면 나중에 화면이 쌓인 뒤 고치는 것보다 훨씬 쉽다.

🔴 **조각 2~4 는 스키마를 건드리지 않는다.** 통로를 열고 화면을 옮길 뿐이다(B-2-b).

---

## I. 하지 않기로 한 것

- **알림을 포털 DB 에 저장하지 않는다** — D-2
- **DB 를 합치지 않는다** — D-3. PO/내자가 마지막 예외다
- **권한을 포털로 옮기지 않는다** — G-3. 옮기면 포털이 모든 업무를 알게 된다
- **역할 어휘를 통일하지 않는다** — F-4. 시스템의 것이지 회사의 것이 아니다
- **`@dss/ui` 가 자료를 가져오게 하지 않는다** — C-4. 시험이 막고, 그 시험이 옳다
- **웹 푸시를 쓰지 않는다** — 사내망 http · 인터넷 없는 NAS 가 전제다
- **A/S 의 확인 기록·알림 설정 표를 옮기지 않는다** — 파생 방식을 유지하므로 그대로 둔다

---

## J. 검증

**조각마다**
- `npm test` · `npm run test:db` · `npx tsc --noEmit` · `npm run lint`
- 화면이 바뀐 조각은 **눈 확인**을 먼저 받는다
- 🔴 `npm run build` 는 dev 서버를 내린 뒤에

**조각 2 (권한 회수)**
- 권한을 뺏은 직후 그 사람의 A/S 세션이 **실제로 끊기는지** 확인한다
- 🔴 **정지 기능이 그대로 도는지** 함께 본다 — 같은 통보 장치를 쓴다

**조각 3 (로그인 후 원래 주소)**
- 로그인하지 않은 상태로 **깊은 주소**를 열었을 때 로그인 뒤 **그 주소로** 도착하는지
- 🔴 **자기 사이트 밖 주소를 넣으면 거절하는지** — 넣어 보고 확인한다(피싱 통로 방지)
- 주소에 물음표·한글·`#` 이 섞여도 온전히 살아 돌아오는지

**조각 5~7 (통합 종)**
- 한 시스템을 **일부러 꺼 놓고** 종이 나머지를 보여주는지
- 포털을 꺼 놓고 **각 사이트가 여전히 열리는지**(종만 비어야 한다)
- 확인한 알림이 **다른 기기의 종에서도** 사라지는지(지금 보장하는 성질이다)
- 한 사람의 확인 기록이 **다른 사람의 종을 지우지 않는지**

**운영 배포 전**
- 🔴 포털은 **모든 시스템의 로그인 통로**다. 포털을 건드리는 조각은 되돌리기 계획을 함께 적는다
