# Security Policy

내부 DSS A/S 관리 시스템의 보안 정책 문서이다. 실제 구현 코드나 설정 파일은 포함하지 않으며, 정책과 원칙만 정의한다.

---

## 1. Authentication (인증)

- 로그인은 회사 이메일 계정 기반으로 한다.
- 신규 가입자는 관리자 승인 전까지 로그인이 제한된다(최초 로그인 전 관리자 승인 필수).
- 로그인 7회 연속 실패 시 계정을 잠금 처리한다.
- 자동 로그아웃(세션 타임아웃) 시간은 관리자가 설정할 수 있다.
- 1차 배포 범위에는 2단계 인증(2FA)을 포함하지 않으나, 향후 2FA를 도입할 수 있도록 인증 아키텍처를 설계한다.
- 로그인 성공/실패 이력은 감사 로그에 기록한다(source_ip, session_id 포함).
- 세션 방식(서버 세션 vs JWT)은 API_SPECIFICATION.md와 함께 추후 확정한다(TBD).

## 2. Authorization (인가)

5개 역할 기반 접근 제어(RBAC)를 사용한다.

| 역할 코드 | 한글 명칭 | 권한 개요 |
|---|---|---|
| SUPER_ADMIN | 최고관리자 | 시스템 전체 설정, 사용자/권한 관리, 전체 데이터 접근, 감사 로그 열람 |
| ADMIN | 관리자 | 전체 업무 데이터 접근/승인, 사용자 승인, 감사 로그 열람 |
| AS_ENGINEER | A/S 엔지니어 | 담당 제품 접수/작업 이력/첨부파일 등록, 검수 요청 |
| SALES | 영업 담당자 | 고객사/견적/PO/출하 승인 관련 데이터 접근 |
| INVENTORY_MANAGER | 재고 담당자 | 인수품 현황, Excel 생성, 출하 관련 데이터 접근 |

- 감사 로그의 `actor_user_id`는 SUPER_ADMIN/ADMIN만 조회 화면에서 확인할 수 있다. 단, 시스템 내부에는 역할과 무관하게 항상 저장된다.
- 출하 승인 위임 지정 권한은 원칙적으로 SUPER_ADMIN이 가지며, ADMIN은 명시적으로 인가된 경우에만 위임을 지정할 수 있다. 인가되지 않은 ADMIN은 자기 자신을 포함해 누구도 위임 승인자로 지정할 수 없다. 위임받는 승인자는 활성 계정이며 출하 승인 자격을 보유해야 한다. 위임은 특정 A/S 건이 아니라 유효 기간 동안 해당 승인자가 처리 가능한 모든 A/S 건에 적용되며, 종료 일시가 지나면 자동 만료된다.
- 출하 완료 후 잠금(`is_locked = true`)된 레코드는 일반 사용자(AS_ENGINEER/SALES/INVENTORY_MANAGER)가 직접 수정할 수 없으며, 잠금 해제 승인 권한은 ADMIN 이상으로 한다.
- 비활성화된 워크플로 단계에 머문 접수 건을 대체 단계로 수동 이동시키는 권한은 ADMIN 이상으로 하며, 이동 시 사유 입력이 필수이다.
- 세부 화면/API 단위 권한 매트릭스는 API_SPECIFICATION.md에서 엔드포인트별로 구체화한다.

### 권한 모델 원칙

- SUPER_ADMIN의 접근 권한도 하드코딩된 예외가 아니라 인가 정책(권한 평가 로직)을 통해 판별한다.
- 사용자별 권한 부여·회수(예: ADMIN에게 위임 지정 권한 부여)는 감사 로그에 기록한다.
- 1차 배포에서는 사용자 단위 권한 플래그(예: `can_assign_shipment_delegation`, `can_approve_shipment`)를 사용하되, 향후 `permissions`/`role_permissions`/`user_permissions` 구조로 확장 가능하도록 설계한다.
- 역할 기반 권한(5개 고정 역할)과 사용자별 예외 권한(개별 사용자에게만 부여되는 권한)을 명확히 구분한다.

## 3. Password Policy (비밀번호 정책)

- 최소 10자 이상, 영문/숫자/특수문자 조합을 권장한다(정확한 정책은 TBD).
- 비밀번호는 해시(bcrypt/argon2 등, 알고리즘 확정은 TBD)로 저장하며 평문 저장·로그 출력을 금지한다.
- 비밀번호 주기적 변경 강제 여부는 TBD.
- 로그인 실패 임계치 및 계정 잠금 정책은 1번 Authentication 항목을 따른다(7회 연속 실패 시 잠금).

## 4. HTTPS

- 모든 통신은 HTTPS(TLS 1.2 이상)를 강제한다.
- HTTP 접근은 HTTPS로 리다이렉트하고, HSTS 적용을 원칙으로 한다.

## 5. VPN

- 내부망 접속을 기본으로 한다.
- 원격지에서는 반드시 회사 VPN을 통해서만 접속한다.
- VPN 이외의 경로를 통한 외부 접근은 허용하지 않는다.

## 6. File Encryption (파일 암호화)

- 첨부파일(사진/문서)은 저장 시 암호화를 원칙으로 한다(암호화 방식·키 관리 체계 TBD, 12번 Storage Architecture와 연계).
- 다운로드 시에도 인가된 사용자만 접근 가능하도록 서명된 URL 또는 서버 프록시 방식을 검토한다(구체 방식 TBD).

## 7. Database Encryption (DB 암호화)

- 저장 데이터 암호화(at-rest encryption)를 적용하는 것을 원칙으로 한다(디스크 레벨 vs 컬럼 레벨 여부 TBD, 12번 Storage Architecture와 연계).
- 비밀번호는 해시 저장이 기본이며, 그 외 민감 정보에 대한 컬럼 단위 암호화 적용 범위는 TBD.

## 8. Backup Policy (백업 정책)

- DB는 최소 1일 1회 이상 백업한다.
- 첨부파일은 DB와 별도로 백업한다.
- 소프트 삭제된 데이터는 30일간 복구 가능해야 한다.
- **RTO(목표 복구 시간)**: 1시간 이내.
- **RPO(목표 복구 시점)**: 하루 전체 업무 데이터 손실은 허용되지 않으므로 24시간 미만이어야 하며, 권장 목표는 1시간이다.
- 일 1회 백업만으로는 위 RPO 목표를 단독으로 충족할 수 없으므로, 일 1회 전체 백업에만 의존하지 않는다.
- 최종 RPO 수치와 상세 증분 백업/트랜잭션 로그 전략은 서버 및 NAS 환경이 확정된 이후 구현 단계에서 결정한다(TBD).
- 복구 절차는 정기적으로 점검(복구 리허설)하는 것을 원칙으로 한다(주기 TBD).
- CLAUDE.md의 "기존 데이터 삭제 또는 초기화 명령을 실행하지 않는다" 규칙에 따라, 백업/복구 관련 명령은 항상 사전 보고 및 승인 후에만 안내한다.

## 9. Audit Log (감사 로그, 보안 관점)

- 아래 15종 행위 유형을 감사 로그 대상으로 한다: LOGIN, CREATE, UPDATE, SOFT_DELETE, RESTORE, STATUS_CHANGE, FILE_UPLOAD, FILE_DOWNLOAD, FILE_DELETE, EXCEL_IMPORT, EXCEL_EXPORT, APPROVE, APPROVAL_CANCEL, ACCOUNT_LOCK, ACCOUNT_DEACTIVATE.
- 감사 로그는 일반 사용자와 관리자 모두에게 append-only이며, 일반 UI를 통한 개별 레코드의 임의 삭제는 허용하지 않는다.
- 보관 기간은 3년으로 한다.
- 보관 기간 경과 후에는 자동 삭제(retention-based deletion)가 발생할 수 있다.
- 예외적인 조기 삭제가 필요한 경우, 문서화된 보안 절차를 거쳐야 하며 별도의 불변 보안 이벤트 레코드를 남긴다. (권장 정책)
- 상세 필드 정의는 DATABASE_DESIGN.md 9번 항목을 따른다.

## 10. Upload Policy (업로드 정책)

- 개별 파일 최대 크기: 300MB.
- 대용량 파일은 분할 업로드(Chunked Upload) 및 재개 가능한 업로드(Resumable Upload)를 지원한다.
- 원본 이미지는 원본 그대로 보관하며, 압축 미리보기 이미지를 별도로 생성·보관한다.
- 파일 삭제 이력은 감사 로그(FILE_DELETE)와 파일 메타데이터의 소프트 삭제 컬럼에 보관한다.
- 파일은 반영구 보관을 원칙으로 한다.
- 퇴사자/비활성화된 계정이 업로드한 파일도 계속 열람·사용 가능하다.
- 파일 접근은 반드시 애플리케이션을 통해서만 이루어지며, 일반적으로 접근 가능한 NAS 공유 폴더 형태로 노출하지 않는다.
- 지원 파일 유형: JPG, PNG, PDF, XLS, XLSX, DOC, DOCX, ZIP, CSV, TXT, 오실로스코프 데이터, 장비 로그 파일, 펌웨어, 회로도.
- 업로드 파일명은 서버에서 안전한 형태로 재생성하고 원본 파일명은 메타데이터로만 보관한다.

## 11. Malware Scan Policy (악성코드 검사 정책)

- 모든 업로드 파일은 저장 전 악성코드 검사를 거치는 것을 원칙으로 한다.
- 검사 엔진/방식은 TBD(예: ClamAV 등 후보 검토).
- 검사 완료 전까지는 다운로드를 제한하고, 악성코드 탐지 시 격리 및 관리자 알림 처리한다(알림 채널 TBD).

## 12. Storage Architecture (스토리지 아키텍처)

- 애플리케이션과 데이터베이스는 온프레미스 서버 또는 서버급 PC에서 운영한다.
- 첨부파일의 보호 저장 및 백업에는 NAS를 사용한다.
- NAS 제조사/모델/용량/RAID 구성/UPS/컨테이너 지원 여부는 TBD.
- 테스트 환경과 운영 환경은 반드시 분리한다.
- NAS 공유 폴더는 일반 사용자에게 노출되는 형태로 공유하지 않으며, 애플리케이션을 통해서만 접근한다(10번 Upload Policy와 연계).
- 정확한 암호화 및 저장 구현 방식은 NAS 및 서버 환경이 확정된 이후 결정한다(TBD).

## 12-1. Idempotency Key Retention (접수 등록 멱등성 키 보관 정책)

- 접수(A/S 접수 등록) 중복 생성 방지를 위한 `repair_case_idempotency_keys` 테이블은 인증/비즈니스 레코드가 아니라 짧은 수명의 운영용 테이블이다(3번 소프트 삭제 4-컬럼 컨벤션을 따르지 않는 이유).
- 키는 클라이언트에서 `crypto.randomUUID()`로 생성하며, 고객/제품/일자 등 어떤 업무 필드로부터도 유도하지 않는다.
- 보관 기간(TTL)은 생성 후 **2시간**을 기본값으로 한다 — 더블클릭/새로고침/네트워크 재시도를 넉넉히 커버하면서, `response_snapshot`(접수 ID·인수번호만 저장, 연락처 등 PII 없음)이 남아 있는 기간을 짧게 제한하기 위함이다.
- 만료된 행은 향후 별도로 구현될 자동 보관기간 만료 삭제(retention-based deletion) 작업으로 제거될 수 있다 — 이는 CLAUDE.md의 "기존 데이터 삭제 또는 초기화 명령을 실행하지 않는다" 규칙의 명시적 예외(시스템 자체의 보관기간 만료 자동 삭제)에 해당한다. 이 자동 삭제 작업 자체의 구현은 별도 승인 대상이며 이 정책 문서는 그 대상과 보관 기간만 정의한다.
- `expires_at` 컬럼은 스키마에 이미 존재하며, 자동 삭제 작업이 구현되기 전까지 만료된 행은 즉시 삭제되지 않고 테이블에 남아있을 수 있다(claim 로직은 상태 값만으로 동작하므로 안전에는 영향 없음).
