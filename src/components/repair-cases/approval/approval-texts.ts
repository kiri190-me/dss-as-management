/**
 * 승인 화면에서 **두 자리가 같은 말을 해야 하는** 문구만 모아 둔다.
 *
 * 🔴 이 파일은 **아무것도 물지 않는다** — 클라이언트 경계 지시문도, 서버 전용
 * 표시를 타는 것도, 화면 라이브러리도 부르지 않는다. import 문이 한 줄도 없다는
 * 것이 그 약속이다. 문구를 최종 출하 승인 카드에 두고 확인 창이 카드를 부르게
 * 하면, 카드가 물고 있는 서버 액션 때문에 사슬이 걸려 **확인 창을 렌더하는 시험이
 * import 단계에서 죽는다**(approval-screen-layout.test.tsx 머리말). 그래서 양쪽
 * 어느 쪽도 아닌 이 자리에 둔다.
 *
 * 🔴 여기에는 **문자열만** 둔다. 판정이나 서식이 끼어들기 시작하면 이 파일이
 * 무언가를 부르게 되고, 위의 이유가 그대로 되살아난다.
 *
 * ⚠️ 화면 배치 시험이 이 파일의 **원본 글자**를 읽어 위 두 약속을 지킨다 — 금지된
 * 것들을 주석에 글자로 적어도 걸린다. 그쪽이 맞다: 약하게 고치지 말고 주석을 바꾼다.
 */

/**
 * 사내 목표 출하일이 **아직 비어 있을 때**의 문구. 최종 출하 승인 카드의 회색
 * 상자와 승인 확인 창이 같은 사실을 말하므로 **글자 그대로 같아야** 한다 — 같은
 * 사실에 두 가지 말이 생기면 사람은 어느 쪽이 맞는지 알 수 없다. 「-」 한 글자로
 * 줄이지 않는 이유도 같다: 어디서 고치는지를 말해 주지 않으면 사람은 그 자리에서
 * 멈춘다.
 *
 * 🔴 값을 고치는 자리는 「접수 정보 편집」 하나뿐이다(IntakeInfoEditForm.tsx
 * 머리말). 카드도 창도 이 값을 읽기만 한다.
 */
export const UNSET_TARGET_SHIPMENT_DATE_TEXT = "아직 정해지지 않았습니다. 접수 정보에서 입력합니다.";
