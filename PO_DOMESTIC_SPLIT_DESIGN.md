# PO / 내자 분리 설계

메뉴 그룹 **「PO / 내자」**(내자 정리 · 견적서 · 작업 비용)를 A/S 관리 시스템에서
떼어 **별도 사이트**로 세운다. 통합로그인에 독립 항목으로 서고, 메뉴바에도 따로 뜬다.

작성 2026-09-20. 조사 근거는 같은 날 돌린 조사 셋이다(내자 범위 · 견적서/작업비용 의존 ·
개선요청 분리 복기). 이 문서는 **설계 승인용**이고, 구현은 조각마다 따로 승인받는다.

---

## A. 왜 하는가

사용자 요구 그대로다 — **「PO/내자를 UI/UX 를 그대로 유지한 채, 통합로그인 단계로 따로
빼 달라. 수리건과의 연계 등은 그대로 유지해 달라.」**

앞서 개선요청 기능이 같은 방식으로 떨어져 나가 `dss-improvements` 가 되었다(2026-09-17,
A/S 쪽 커밋 `ac22e09`). 이번은 그 두 번째다. 다만 **성격이 전혀 다르다** — D절을 볼 것.

---

## B. 정해진 것 (2026-09-20 사용자 결정)

| # | 항목 | 결정 | 근거 |
|---|---|---|---|
| 1 | 범위 | **메뉴 그룹 전부** — 내자 정리 · 견적서 · 작업 비용 | 「견적은 내자 진행 상황표 바로 앞에 오는 일이고 두 화면을 오가는 사람이 같다」(`navigation.ts:164-166`) |
| 2 | DB | **같은 `dss_as` 를 둘이 본다** | 「연계를 그대로 유지」와 DB 분리는 동시에 성립하지 않는다. D절 |
| 3 | 스키마 공유 | **서브모듈** | 손으로 베낀 복제본은 컴파일러가 지켜 주지 않는다. 이미 그런 것이 넷 있다 |
| 4 | 견적서 결재선 설정 화면 | **A/S 에 그대로 두고, 새 사이트에서는 링크로 보낸다** | 출하·불출과 한 화면에서 관리하는 편이 낫다. 관리자만 쓰는 화면이라 건너가는 불편이 작다 |
| 5 | 견적서 결재 알림(종) | **A/S 종에 계속 띄운다** | 같은 DB 라 가능하다. 사람은 종 하나만 보면 된다. 링크 주소만 새 사이트로 바꾼다 |
| 6 | 수리건 상세의 [견적서] 탭 | **지금 모습 그대로 유지** | E절의 방법으로 「복사본이 아니라 한 벌을 나눠 쓰는」 형태로 만든다 |
| 7 | 진행 방식 | **조각으로 나눠서** | 73,000줄을 한 번에 옮길 수 없다. 조각마다 커밋하고 중간에 멈춰도 쓸 수 있어야 한다 |

---

## C. 규모 (실측)

| | 줄 수 | 파일 |
|---|---:|---:|
| 내자 정리 | 16,100 | 35 |
| 견적서 | 51,681 | 144 |
| 작업 비용 | 5,395 | 13 |
| **옮길 것 합계** | **약 73,000** | **192** |
| 갈라내거나 나눠 써야 할 공유 코드 | 약 11,100 | — |

참고 — **개선요청 사이트 전체가 20,023줄**이었다. 이번은 그 **4배**이고, 개선요청은
아무와도 얽혀 있지 않았지만 이번은 얽혀 있다.

---

## D. 🔴 이것은 예외다 — 새로 생기는 시스템은 이렇게 하지 않는다

**DB 를 함께 보는 것은 이번만이다.** 앞으로 더해질 시스템이 정보 일부를 주고받아야
할 때는 **API 로 잇는다.** 이 문서를 본보기로 삼아 DB 를 계속 합치면 안 된다.

**DB 를 함께 봐야 하는 경우는 셋뿐이다:**

1. 한 화면이 양쪽 자료를 **섞어서 정렬·검색·쪽 넘김** 해야 할 때
2. 한 트랜잭션 안에서 양쪽을 **함께 바꿔야** 할 때
3. **DB 가 강제하는 참조 무결성(FK)** 이 필요할 때

내자는 1·3에 정통으로 걸린다. 내자 목록 화면은 `repair_cases`·`products`·`customers`
×2·`quotes` 를 **한 질의에 LEFT JOIN** 해서 그리고(`queries/domestic-orders.ts:522-531`),
수리건을 지우면 `ON DELETE SET NULL` 이 연결을 자동으로 푼다. 나누면 고아 참조가
조용히 남는다 — 스키마 주석이 길게 막으려 한 바로 그 사고다.

**그 셋이 아니면 API 가 낫다.** 검색·정렬이 필요하면 **필요한 조각만 자기 DB 에 사본으로
두는** 가운데 길이 있다(주인은 한 곳, 사본은 여럿). 마스터 자료(고객사·제품모델·사용자)가
그 부류다.

**DB 를 계속 합치면 치르는 값**: 마이그레이션과 배포가 묶인다. 칸 하나를 바꾸면 그 DB 를
보는 앱을 **전부 같이 고쳐 같이 올려야** 한다. 앱이 둘이면 견디지만 다섯이면 못 견딘다.

---

## E. 구조

### E-1. 저장소 셋

```
RF_Service_System  (A/S 관리, 3000)  ─┐
                                      ├─→ 같은 DB  dss-pg-app / dss_as
dss-po             (PO·내자, 3600)   ─┘
                    ↑
              vendor/dss-core   ← 새 서브모듈 (스키마 + 나눠 쓰는 코드)
              vendor/dss-ui     ← 기존 서브모듈 (메뉴바)
```

포트 3600, 도메인 `po.dss21.co.kr`, 이미지 `dss-po`. 개발 DB 는 **새로 만들지 않는다** —
A/S 와 같은 `dss-pg-app`(5442)의 `dss_as` 를 본다.

### E-2. `vendor/dss-core` — 새 서브모듈

`dss-ui` 와 같은 방식이다(git 서브모듈 · tsconfig 별칭 · eslint 무시 한 줄). 그 방식이
선택된 까닭은 각 사이트 Dockerfile 이 `COPY . .` 라 **저장소 폴더 안**에 있어야 이미지에
담기기 때문이다 — 이미 검증된 길이다.

**담을 것**

| 무엇 | 지금 자리 | 규모 |
|---|---|---|
| Drizzle 스키마 전부 | `RF_Service_System/src/lib/db/schema/` | 63파일 9,127줄 |
| 견적서 목록 화면 | `components/quotes/QuoteListScreen.tsx` | 814줄 |
| 그 화면이 쓰는 공용 조각 | `responsive-list` · `SavePopup` · `master-data-trash-*` 등 | — |
| 양쪽이 쓰는 도메인 순수 함수 | `quote-list.ts` · `date-only.ts` · `amount-input.ts` 등 | — |

**담지 않을 것**

- **마이그레이션.** `drizzle/` 과 `drizzle.config.ts` 는 **A/S 가 계속 소유**한다.
  새 사이트에는 `db:generate` · `db:migrate` 스크립트를 **두지 않는다.**
  칸을 더하거나 고치는 일은 언제나 A/S 에서 한다.
- **엑셀 기반 4,441줄.** 초기에는 **복제**한다. 서브모듈이 자리 잡은 뒤 다시 본다 —
  한 번에 둘을 빼면 되돌리기가 어렵다.

🔴 **A/S 도 수술을 받는다.** `src/lib/db/schema/` 63파일이 서브모듈로 옮겨가고, A/S 는
거기서 re-export 한다. `drizzle.config.ts` 가 서브모듈 경로를 가리키게 고친다.
**조각 0 에서 이것부터 하고, 옮긴 직후 전체 회귀를 한 번 돌린다.**

### E-3. 인증 · 메뉴바

`dss-improvements` 를 그대로 베낀다(복기 문서에 절차가 있다). 쿠키 이름만 가른다 —
`po_session` · `po_sso_tx` · `po_service_menu`. 🔴 **쿠키는 포트를 가리지 않으므로**
A/S 와 같은 이름을 쓰면 한쪽 로그인이 다른 쪽을 덮는다(A/S 에서 실제로 났던 구멍).

포털 등록은 `npm run client:register`(개발) / SQL INSERT(운영 NAS — 그쪽엔 tsx 가 없다).

### E-4. 파일 저장소

🔴 **두 사이트가 같은 `UPLOADS_DIR` 을 가리켜야 한다.** 첨부 표가 하나이므로, 갈라지면
견적서 첨부가 A/S 에서 안 열린다. NAS 에서는 같은 볼륨을 두 컨테이너에 마운트한다.

---

## F. 어려운 문제 여섯과 그 답

| # | 문제 | 답 |
|---|---|---|
| 1 | **결재선을 세 결재가 같이 쓴다.** `shipment_approval_routes` 에 출하·불출·견적서가 `scope` 로 얹혀 있고, 설정 화면은 A/S 의 [사용자 관리] 안 | **A/S 에 그대로 둔다.** 새 사이트의 결재 화면에서 「결재선 설정」을 누르면 A/S 로 넘어간다. 읽기 쿼리만 서브모듈로 나눠 쓴다 |
| 2 | **알림 종이 쪼개진다.** 「승인됨」·「반려됨」이 네 결재를 한 종류로 묶고 있다 | **A/S 종에 계속 띄운다.** 같은 DB 라 `quote_approvals` 를 그대로 읽는다. 링크 주소만 새 사이트로 바꾼다 |
| 3 | **A/S 가 견적서 부품 고르개를 쓴다.** `UsedPartsEditForm` 이 `quote-part-picker` 를 import 하고, A/S 시험이 그 import 줄을 정규식으로 박아 뒀다 | 🔴 **공용 묶음(`vendor/dss-core`)으로 보냈다 — 2026-09-22 완료**(A/S `a05ac46` · dss-core `d805b39`). 지금 경로는 `@dss/core/ui/inventory/part-picker` 다. 처음에는 **A안**(「A/S 쪽으로 옮기고 이름을 바꾼다」)으로 적었고 같은 날 **B안**으로 뒤집었다 — 바로 아래 F-3 |
| 4 | **공용 알림 파일이 견적서 파일을 import** 한다(`domain/notifications.ts:30`) | 링크 만드는 함수 한 줄이므로 **공용 쪽으로 복사**한다 |
| 5 | **수리건 상세의 [견적서] 탭.** 목록 화면 814줄을 두 사이트가 나눠 쓸 수 없다 | **`vendor/dss-core` 에 넣어 한 벌을 나눠 쓴다.** 복사본이 아니므로 「금액·요약 줄이 갈라지는 날」이 오지 않는다 — 그 파일 주석이 경고한 것이 바로 그것이다 |
| 6 | **엑셀 기반 4,441줄**을 견적서와 A/S 수리보고서·교산 인수품 가져오기가 함께 쓴다 | 초기에는 **복제**. 서브모듈이 자리 잡은 뒤 다시 본다 |

### 🔴 F-3 — A안으로 적었다가 B안으로 뒤집었다 (2026-09-22 사용자 결정)

이 표의 3번은 처음에 **A안**이었다: 「**그 파일을 A/S 쪽으로 옮기고 이름을 바꾼다** —
원래 부품 고르는 일이지 견적서 일이 아니다. 시험의 정규식도 함께 고친다.」 그 사실을
지우지 않고 적어 둔다 — 「왜 설계서와 다르게 했나」를 다음에 읽는 사람이 물어볼
자리이기 때문이다.

**뒤집은 까닭**

- 🔴 **그 조각은 A/S 도 PO 도 쓴다.** A안으로 A/S 에 두면 PO 가 A/S 를 import 해야 하고,
  그것이 바로 이 분리로 끊으려는 줄이다. 이름만 견적서였다는 진단(A안의 근거)은 맞았지만,
  그렇다면 갈 곳은 A/S 가 아니라 **양쪽이 함께 보는 자리**다 — 로직에 견적서 개념이
  하나도 없다(견적서 종류도 양식도 `quote_items` 도 없다).
- **이미 갈라진 자리가 있다.** `vendor/dss-core` 는 조각 0a 로 이미 서 있다. 새 길을
  여는 일이 아니라 있는 길에 파일 셋을 더하는 일이었다.
- 🔴 **작은 파일로 그 길을 먼저 열어 보는 것이 안전하다.** 공용 묶음에 **화면 조각**
  (`"use client"` · react)을 넣는 첫 시도였고, 이 조각에서만 잡힌 것이 둘이다 — ① A/S 의
  `vendor/dss-core` 포인터가 **화면 폴더가 아예 없는 옛 커밋**을 가리키고 있었다(PO 는
  앞서 있었다). ② A/S 의 tsconfig include 가 서브모듈을 컴파일 대상에 넣으므로, 포인터를
  맞추는 것만으로 공용 화면 파일 7개가 **처음으로 A/S tsc 에 실렸다.** F-5(견적서 목록
  화면 814줄)로 이 길을 처음 열었다면 같은 문제를 훨씬 큰 조각 안에서 만났을 것이다.
- F-5 가 이미 「`vendor/dss-core` 에 넣어 한 벌을 나눠 쓴다」이므로, 3번도 B안으로 두면
  **같은 종류의 문제에 같은 답**이 된다.

**A안의 대가**(고르지 않은 쪽): 파일이 A/S 에 남아 PO 가 A/S 를 import 하게 되거나,
아니면 PO 쪽에 복제본이 생긴다. 복제본은 컴파일러가 지켜 주지 않는다(B절 3번의 그 까닭).

**실제로 한 일**: `vendor/dss-core/src/ui/inventory/` 에 파일 셋 —
`part-picker.tsx` · `part-picker-rows.ts` · `part-price-field.ts`(옛
`src/lib/domain/quote-part-price.ts`. 고르개의 유일한 값 의존이라 함께 갔다).
형 둘(`PartPickerRow` · `PartPickerPriceRow`)은 `queries/inventory.ts` 가 **옛 선언이
있던 자리에서 재수출**하므로 A/S 안의 기존 import 는 한 줄도 바뀌지 않았고, 고르개를
직접 부르는 두 파일(`QuoteEditForm` · `UsedPartsEditForm`)만 경로가 바뀌었다. 시험은
**A/S 에 남겼다**(`src/components/inventory/part-picker.test.tsx`) — 공용 묶음에는 시험
러너가 없고, A/S 의 등록 감시가 최상위 `vendor/` 를 건너뛰어 거기 두면 아무 소리 없이
영영 안 돈다. 행동은 한 글자도 바뀌지 않은 순수 이동이다(시험 개수가 기준선과 같다 —
4381 · 1369, PO 684).

### 🔴 이름으로는 못 찾는 것들 (옮길 때 반드시 확인)

- 견적서 표 `quote_repair_tasks` 가 **작업비용 스키마 파일 안**에 있다(`schema/repair-labor.ts:358`)
- 작업비용 검증 파일은 이름에 labor 가 없다(`validation/repair-task-input.ts`)
- `domain/quote-link-options.ts` 는 이름만 quote 이고 **내자 전용**
- `domain/repair-case-link-search.ts` 는 이름만 repair-case 이고 **내자 전용**
- `components/common/AmountInput` · `copy-text` · `domain/amount-input` 은 이름이 common 인데 **소비자가 견적서·작업비용뿐** — 통째로 가져가도 A/S 는 아무것도 잃지 않는다
- `components/repair-cases/detail/edit/EditSectionActions` 는 이름만 repair-case 이고 **견적서 폼·작업비용 화면이 쓴다**
- `scripts/load-template-env.ts:23-30` 이 견적서 양식 5개와 A/S 수리보고서 양식 2개를 **한 배열에** 담고 있다 — 갈라야 한다
- 🔴 **부품 단가 규칙의 시험이 이름이 전혀 다른 파일 안에 있다.** `part-price-field.ts`
  (옛 `domain/quote-part-price.ts`)의 `toPriceFieldValue` · `isPriceUnset` 시험은
  `src/lib/domain/quote-labor-cost.test.ts` 안에 있다. F-3 으로 그 파일이 공용 묶음으로
  옮겨 간 뒤에도 시험은 A/S 에 남았다 — 공용 묶음에 시험 러너가 없기 때문이다. 파일
  이름으로 찾으면 「시험이 없는 파일」로 보인다.
  (⚠️ 그 시험 파일 주석은 새 파일을 만들 수 없는 까닭으로 「`package.json` 의 `test`
  줄이 cmd.exe 명령줄 한도 8191자에 닿는다」를 적어 두었는데 **지금은 옛말이다** —
  시험 등록이 `scripts/test-lists/*.txt` 로 옮겨 갔다. 그래도 시험을 옮길 까닭은 없다)

---

## G. 조각 순서

각 조각은 **혼자 커밋할 수 있고, 거기서 멈춰도 시스템이 돈다.**

| 조각 | 내용 | 위험도 |
|---|---|---|
| **0a** | `dss-core` 저장소를 만들고 A/S 스키마를 옮긴다. A/S 는 re-export. **전체 회귀 1회** | 🔴 HIGH |
| **0b** | `dss-po` 저장소 뼈대 — package.json · next.config(`allowedDevOrigins: ["192.168.*.*"]`) · tsconfig · eslint · 인증 5파일 · 메뉴바 · 시험 러너. 로그인하면 빈 화면 | MEDIUM |
| **0c** | 포털에 `dss-po` 등록(개발). 로그인이 되고 메뉴바에 뜬다 | MEDIUM |
| **1** | **작업 비용** 옮기기 — 가장 깨끗하다(견적서 말고 아무도 안 읽는다). 첫 실물 화면 | LOW |
| **2** | **내자 정리** 옮기기 | MEDIUM |
| **3a~3f** | **견적서** — 목록·휴지통 → 편집 폼 → 엑셀 양식·내려받기·발행 → 첨부 → 결재 → 인쇄·미리보기 | MEDIUM~HIGH |
| **4** | A/S 정리 — 메뉴에서 빼고, 남은 참조를 링크로 바꾸고, 안 쓰는 파일을 지운다 | 🔴 HIGH |
| **5** | 배포 — `dss-deploy` 에 compose·env·백업 두 줄·런북. DNS·리버스 프록시·포털 등록은 사람이 | 🔴 HIGH |

🔴 **조각 4 전까지는 A/S 에도 화면이 그대로 남아 있다.** 두 곳에서 같은 자료를 고칠 수
있다는 뜻이고, 같은 DB 라 자료가 갈라지지는 않는다. 조각 4 에서 A/S 쪽을 걷어낸다.

---

## H. 하지 않기로 한 것

- **DB 를 나누지 않는다** — B-2
- **자료를 옮기지 않는다** — 같은 DB 라 옮길 것이 없다(개선요청과 가장 다른 점이다)
- **새 마이그레이션을 새 사이트에서 만들지 않는다** — E-2
- **썸네일을 새로 만들지 않는다** — 이번 분리와 무관
- **견적서 결재가 발행을 막게 하지 않는다** — 사용자 결정이 스키마 주석에 박혀 있다
  (`schema/quote-approvals.ts:25-31`). 옮길 때 그 주석도 함께 옮긴다

---

## I. 검증

**조각마다**
- `npm test` · `npm run test:db` · `npx tsc --noEmit` · `npm run lint`
- 화면이 바뀐 조각은 **눈 확인**을 먼저 받는다
- 🔴 `npm run build` 는 dev 서버를 내린 뒤에

**조각 0a 는 특별하다** — 스키마가 통째로 움직이므로 A/S 전체 회귀를 돌린다.

**조각 4 이후**
- A/S 에 깨진 import 가 없는지
- 수리건 상세의 [견적서] 탭이 그대로 도는지
- 결재 알림이 A/S 종에 뜨고 링크가 새 사이트로 가는지
- 주간보고의 견적발행일·발주발행일이 그대로 차는지
- 「장기 PO 미발행만」 필터가 그대로 도는지
- 고객 안내 현황판의 견적서번호·발행일이 그대로 차는지

**운영 배포 전**
- 🔴 운영 DB 백업. 조각 0a 는 스키마 파일만 옮기고 표는 안 건드리지만, 조각 4 가
  A/S 쪽 코드를 지운다
- 두 컨테이너가 **같은 업로드 볼륨**을 보는지 실제로 확인
