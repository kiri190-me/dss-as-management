# UI Guideline

내부 업무용 시스템(DSS A/S 관리 시스템)의 UI/UX 가이드라인이다. 실제 컴포넌트 코드는 포함하지 않으며, 디자인 원칙과 규칙만 정의한다.

---

## 1. Layout

- 표준 레이아웃: 좌측 고정 사이드바(내비게이션) + 상단바(사용자 정보/알림/로그아웃) + 메인 콘텐츠 영역
- 내부 업무 시스템 특성상 정보 밀도를 우선하며, 마케팅형 랜딩 레이아웃은 사용하지 않는다.
- 메인 콘텐츠 영역은 데스크톱 사용을 전제로 화면 폭을 넓게 활용한다.

## 2. Navigation

- 1차 메뉴 구성(Phase 1 화면 기준): 대시보드, 전체 현황, 제품 접수, A/S 상세, 작업 이력, 파일 관리, 검수/승인, 일본 본사 Excel 생성
- 역할별 메뉴 노출 원칙 (5개 역할):

| 역할 코드 | 한글 명칭 | 주요 접근 메뉴 |
|---|---|---|
| SUPER_ADMIN | 최고관리자 | 전체 메뉴 + 시스템/사용자 설정 |
| ADMIN | 관리자 | 전체 업무 메뉴, 시스템 설정 일부 제한 |
| AS_ENGINEER | A/S 엔지니어 | 제품 접수, A/S 상세, 작업 이력, 파일 관리, 검수/승인(요청) |
| SALES | 영업 담당자 | 고객사, 견적/PO, 출하 승인 관련 화면 |
| INVENTORY_MANAGER | 재고 담당자 | 인수품 현황, Excel 생성, 출하 관련 화면 |

- 위 매핑은 초기 원칙이며, 세부 화면 단위 권한은 API_SPECIFICATION.md의 권한 표와 함께 확정한다.

## 3. Table style

- 목록 화면은 테이블 컴포넌트를 기본으로 하며, 페이지네이션을 사용한다(무한 스크롤 지양).
- 상태값은 텍스트가 아닌 배지(Badge) 형태로 표시하고 7번 색상 규칙을 따른다.
- 정렬 가능한 컬럼은 헤더 클릭으로 정렬한다.
- 전체 현황 화면의 기본 컬럼: 인수번호, 고객사명, 상태, 접수일, 담당 엔지니어
- 소프트 삭제된 레코드는 목록에서 기본적으로 숨기되, 필요 시 "삭제됨" 필터로 조회할 수 있다.

## 4. Form style

- 라벨은 입력 필드 상단에 배치한다.
- 필수 입력 항목은 라벨 옆에 `*` 표시.
- 입력 검증 오류는 필드 하단에 즉시 표시하고, 저장 버튼은 검증 통과 전까지 비활성화한다.
- 인수번호와 같은 시스템 자동 발급 필드는 읽기 전용으로 표시하고 사용자가 직접 수정할 수 없다.

## 5. Dialog style

- 단순 확인(정보성)과 위험 동작(삭제/승인 취소 등) 다이얼로그를 시각적으로 구분한다.
- 위험 동작 다이얼로그는 위험 버튼 색상과 함께 재확인 문구를 포함한다.
- 검수/승인, 출하 승인, 잠금 해제 수정 요청처럼 되돌리기 어려운 동작은 반드시 다이얼로그를 통한 2단계 확인을 거친다.
- 잠금 해제 수정 시 사유 입력 필드는 다이얼로그 내 필수 항목으로 표시한다.

## 6. Button rules

- 주 버튼(Primary): 화면당 1개 원칙(가장 중요한 동작)
- 보조 버튼(Secondary): 취소, 뒤로가기 등
- 위험 버튼(Danger): 삭제성 동작(소프트 삭제 포함), 승인 반려 등 — 별도 색상 사용
- 승인/발급성 버튼(검수 승인, 출하 승인, 인수번호 발급 등)은 중복 클릭 방지를 위해 처리 중 상태(로딩)를 표시하고 버튼을 비활성화한다.
- 출하 완료 후 잠금된(`is_locked = true`) 레코드의 일반 수정 버튼은 비활성화하고, "수정 요청" 버튼만 노출한다.

## 7. Color rules

- 상태값별 색상 매핑 원칙만 정의하고, 실제 팔레트(HEX)는 구현 단계에서 확정한다(TBD).
  - 접수/대기: 중립색
  - 진행 중: 강조색(주의를 끌되 경고 아님)
  - 승인/완료: 성공색
  - 반려/오류: 위험색
  - 예외 상태(보류/대기류): 별도의 경고색 계열로 워크플로 진행 색상과 구분
- 색상만으로 상태를 구분하지 않고, 텍스트 라벨을 항상 함께 표기한다(색약 사용자 고려).

## 8. Typography

- 제목(H1~H3), 본문, 캡션/보조 텍스트의 3단계 위계를 기본으로 한다.
- 숫자 데이터(접수일, 금액 등)는 표 정렬이 잘 되는 폰트 사용을 권장한다(구체 폰트명 TBD).
- 인수번호(예: "D260801")처럼 한글/영문/숫자 혼용이 잦으므로, 한글과 영숫자 모두 가독성이 검증된 폰트를 사용한다(구체 폰트 후보 TBD).

## 9. Dark mode

- 다크모드를 지원하는 것을 원칙으로 하되, 기본(default) 테마는 라이트 모드로 한다(내부 사무 환경 기준).
- 상태 배지 색상은 라이트/다크 모드 모두에서 대비 기준을 만족해야 한다(구체 대비 수치 TBD).

## 10. Mobile layout

- 이 시스템은 데스크톱 사용을 1순위로 설계한다.
- A/S Engineer가 현장에서 사용할 가능성이 있는 화면(작업 이력 입력, 사진 첨부)은 모바일 반응형을 우선 지원한다.
- 그 외 데이터 밀도가 높은 화면(대시보드, 전체 현황 등)은 모바일에서 "조회 전용" 축소 레이아웃만 최소 지원한다.

## 11. Korean UI wording policy (언어 정책)

- 웹 UI의 모든 라벨, 메뉴, 버튼, 검증 메시지, 알림, 오류 메시지는 한국어로 표시한다.
- 소스 코드 식별자, 데이터베이스명, API 경로, 함수명, 변수명은 영어를 사용한다(UI 표시와는 별개).
- 교산 본사 제출용 Excel 출력물은 일본어로 작성하며, 표준 기술 일본어 용어는 내부 용어사전(Terminology Dictionary)을 통해 관리한다.
- 번역되지 않은 영어 개발자 메시지(스택 트레이스, 원시 예외 메시지 등)를 일반 사용자 화면에 노출하지 않는다. 모든 오류는 한국어 사용자 메시지로 변환하여 표시한다.

PROJECT_REQUIREMENTS.md에서 확정한 용어를 화면 라벨의 표준으로 고정하고 동의어를 혼용하지 않는다.

| 표준 용어 | 비고 |
|---|---|
| A/S 접수 / 제품 접수 | 시스템 등록 기능/메뉴명 (동의어 병행 허용) |
| 제품 인수 | 장비를 물리적으로 넘겨받는 행위 (워크플로 1단계) |
| 인수점검 | 인수 직후 점검 워크플로 단계. "접수 검사", "인수 검사" 등 대체 표현 금지 |
| 인수번호 | 형식: D+연도2자리+월2자리+순번2자리 |
| 작업 이력 | |
| 상태 변경 이력 | |
| 검수/승인 | |
| 교산 출하 승인 증빙 | |
| 대표 또는 대행자 출하 승인 | |
| 감사 로그 | |
| 백업 및 복구 | |
| 삭제됨 | 소프트 삭제된 레코드의 화면 표시 문구. 워크플로 예외 상태와는 별개 |

- 역할명은 영문 코드(SUPER_ADMIN 등)를 내부 값으로 쓰되, 화면 표기는 아래 한글 명칭을 사용한다.

| 역할 코드 | 화면 표기(한글) |
|---|---|
| SUPER_ADMIN | 최고관리자 |
| ADMIN | 관리자 |
| AS_ENGINEER | A/S 엔지니어 |
| SALES | 영업 담당자 |
| INVENTORY_MANAGER | 재고 담당자 |
