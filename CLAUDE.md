# Claude Code Rules

## 작업 규칙

- 코드/파일 수정 전에 변경 대상과 변경 계획을 먼저 설명한다.
- 사용자 승인 없이 코드나 문서를 수정하지 않는다.
- 한 번에 하나의 기능만 작업한다.
- 기존 기능을 임의로 삭제하지 않는다.
- Git Push 는 자동 허용돼 있다(2026-08-28 사용자 결정, `.claude/settings.local.json`).
  승인을 따로 받지 않는 대신 **푸시 직전에 무엇이 올라가는지 확인해 보고한다** —
  올라갈 커밋 목록과 `git diff --stat origin/main..main` 의 파일 목록. 우리 것이 아닌
  파일이 섞여 있으면 **올리지 말고 먼저 알린다**(두 세션이 같은 저장소를 쓰므로 실제로
  일어난다 — HANDOFF A절).
- AGENTS.md와 HANDOFF.md는 사용자가 명시적으로 포함을 승인하지 않는 한 git add/staging 대상에서 제외한다.

## 작업 효율 규칙 (토큰 절감)

- 작업 범위는 위험도에 따라 조정한다:
  - **LOW** (독립적 로직, 소규모 UI, 추가적 read-side 변경, 테스트 전용 변경): 관련 파일만 확인, 타겟 테스트만 실행, 전체 저장소 감사 생략.
  - **MEDIUM** (서버 mutation, 인가, 트랜잭션, 감사 이력, 워크플로 의미 변경): mutation + 직접 관련된 schema/types/auth/tests만 확인. 구체적 의존성이 확인될 때만 범위를 넓힌다. 개발 중에는 타겟 통합 테스트, 일관된 작업 단위 완료 시 전체 회귀 1회.
  - **HIGH** (마이그레이션, 마이그레이션 적용, 파괴적 DB 작업, 실 데이터 변경, 권한 확장, 배포/보안, staging/commit 경계): 명시적 preflight, 작업 직전 실시간 검증, 불일치 시 STOP, 작업 후 결과 재확인.
- 저장소를 기본적으로 광범위하게 스캔하지 않는다. 시작점은 현재 작업, HEAD, git diff, 직접 관련 파일/테스트, CLAUDE.md, HANDOFF.md. 구체적 의존성/모호함/테스트 실패/안전 불변식이 있을 때만 범위를 넓힌다.
- 이미 검토·테스트·커밋된 동작은 재감사하지 않는다. 변경되지 않았다면 "승인된 시나리오 전체 통과"로 요약하고 개별 재검증하지 않는다.
- 매 작은 수정마다 전체 테스트 스위트를 재실행하지 않는다: 변경 → 타겟 테스트 → 다음 변경 → 타겟 테스트, 일관된 단위 완료 시 tsc + 전체 필수 회귀 1회 + build + lint.
- LOW/MEDIUM 위험도 작업 보고는 다음만 포함한다: 변경 파일, 구현된 동작, 테스트/합계, 스키마/마이그레이션 영향, 예상치 못한 발견/위험, git status. 원래 작업 지시사항을 반복하지 않고, 필요하지 않으면 전체 소스 파일을 출력하지 않는다.
- 이 규칙은 데이터 안전, 인가 경계, 마이그레이션 절차, 회귀 보호, 승인 게이트를 약화시키지 않는다 — 위험한 작업(마이그레이션 적용, 실 데이터 변경, staging, commit, push, 배포, 권한 확장, 대량 삭제)은 여전히 명시적 사용자 승인이 필요하다.

## DB/데이터 규칙

- DB 스키마 변경 전 영향 범위와 Migration 계획을 먼저 보고한다.
- 기존 데이터 삭제 또는 초기화 명령을 실행하지 않는다.
- 위 삭제 금지 규칙은 Claude가 임의로 실행하는 작업에 적용된다. 시스템 자체의 보관기간 만료 자동 삭제(감사 로그 3년 보관 등, SECURITY_POLICY.md 참조) 등 문서화된 정책에 따른 자동화 절차는 별도로 취급한다.
- Mock Data와 실제 운영 데이터를 명확히 분리한다.

## 배포 규칙

- 운영 배포는 Claude가 직접 수행하지 않는다.
- Claude는 배포 절차와 명령어만 작성한다.
- 테스트 서버 검증 및 관리자 승인 후에만 운영 배포 단계를 안내한다.

## 보안 규칙

- .env 내용을 출력하지 않는다.
- 실제 고객 정보, 회로도, 펌웨어, 비밀번호, API Key를 외부 서비스로 전송하지 않는다.